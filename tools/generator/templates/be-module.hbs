import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { {{pascalCase name}}sController } from './{{dashCase name}}s.controller';
import { {{pascalCase name}}sRepository } from './{{dashCase name}}s.repository';

/**
 * [GEN] Nhớ import vào app.module.ts (checklist bước 5).
 * AuthModule cấp AbilityService — controller cần nó để áp row-level scope.
 */
@Module({
  imports: [AuthModule],
  controllers: [{{pascalCase name}}sController],
  providers: [{{pascalCase name}}sRepository],
  exports: [{{pascalCase name}}sRepository],
})
export class {{pascalCase name}}sModule {}
