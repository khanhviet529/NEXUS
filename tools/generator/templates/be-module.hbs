import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { {{plural (pascalCase name)}}Controller } from './{{plural (dashCase name)}}.controller';
import { {{plural (pascalCase name)}}Repository } from './{{plural (dashCase name)}}.repository';

/**
 * [GEN] Nhớ import vào app.module.ts (checklist bước 5).
 * AuthModule cấp AbilityService — controller cần nó để áp row-level scope.
 */
@Module({
  imports: [AuthModule],
  controllers: [{{plural (pascalCase name)}}Controller],
  providers: [{{plural (pascalCase name)}}Repository],
  exports: [{{plural (pascalCase name)}}Repository],
})
export class {{plural (pascalCase name)}}Module {}
